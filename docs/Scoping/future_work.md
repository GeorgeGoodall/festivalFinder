# Batch add: 
    - upload a list of urls and have it scrape them all one by one

# Update action
    - action on admin, kicks off update
    - only rescan if lineup page or poster has changed
    
# rescrape action
    - action on existing festival
    - rescrape, merge artists reobtain missing fields i.e.:
        - description
        - title
        - price
        - afiliate link

# If we aren't sure about a lineup poster, then we shouldn't select or get one. 
    - should we scan our db and make sure all our existing posters are on a dedicated page?